╔══════════════════════════════════════════════════════════════════════════╗
║                         🔐 @EnableWebSecurity in Spring Boot              ║
╚══════════════════════════════════════════════════════════════════════════╝

🧩 1. What It Is
@EnableWebSecurity is a Spring annotation that tells Spring Boot:

👉 “Hey, turn on Spring Security for this web application and let me customize it., without this as well you have
basic security features, but inorder to customize it you need this.”

──────────────────────────────────────────────

⚙️ 2. What It Actually Does
When you put:
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    // your security settings
}

Spring:
• Enables Spring Security’s filter chain (intercepts all HTTP requests)
• Applies default security (like login form, authentication, etc.)
• Lets you override or customize security behavior (via SecurityFilterChain bean)

Basically, it activates the whole Spring Security system that protects your app.

──────────────────────────────────────────────

🔐 3. What Happens Under the Hood
When @EnableWebSecurity is used, Spring:
1️⃣ Creates a SecurityFilterChain bean — a chain of filters that sit in front of your app.
2️⃣ Every HTTP request passes through this chain.
3️⃣ These filters decide:
   • Is the user authenticated?
   • Do they have permission to access this resource?
   • Should they be redirected to login?

So, @EnableWebSecurity is the main switch that turns on this mechanism.

──────────────────────────────────────────────

🧱 4. Example Usage
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/public/**").permitAll()
                .anyRequest().authenticated()
            )
            .formLogin(withDefaults())
            .logout(withDefaults());
        return http.build();
    }
}

✅ What’s happening:
• @EnableWebSecurity enables Spring Security.
• SecurityFilterChain defines the access rules (which URLs require login, etc.).

──────────────────────────────────────────────

💡 5. In Short
──────────────────────────────────────────────
| Term                  | Meaning                                   |
|-----------------------|-------------------------------------------|
| @EnableWebSecurity    | Enables Spring Security for web apps      |
| Where to use it       | On a @Configuration class (SecurityConfig)|
| What it does          | Activates the security filter chain       |
| Works with            | HttpSecurity, SecurityFilterChain, etc.   |
──────────────────────────────────────────────

──────────────────────────────────────────────
🧠 Think of It Like:
@EnableWebSecurity = “Turn on the Spring Security system for my web app, and let me configure it.”
──────────────────────────────────────────────
