═══════════════════════════════════════════════════════════════════
💡 SPRING SECURITY: @Bean userDetailsService() EXPLAINED
═══════════════════════════════════════════════════════════════════

🔹 METHOD:
@Bean
public UserDetailsService userDetailsService(){
    UserDetails user1 = User.withUsername("user1")
            .password("{noop}password1")
            .roles("USER")
            .build();

    UserDetails admin = User.withUsername("admin")
            .password("{noop}password2")
            .roles("ADMIN")
            .build();

    return new InMemoryUserDetailsManager(user1, admin);
}

───────────────────────────────────────────────────────────────
🧠 EXPLANATION:
───────────────────────────────────────────────────────────────
✅ 1️⃣  @Bean
➡ whatever that method returns — Spring treats it as a **Bean**.

✅ 2️⃣  UserDetails
➡ Represents a single user’s credentials and authorities (roles).
   Example:
   - Username: user1
   - Password: password1
   - Role: USER

✅ 3️⃣  {noop}
➡ Means *“No encoding”* for the password (used for testing/demo purposes).
   🔒 In real applications, use a proper password encoder (e.g., BCrypt).

✅ 4️⃣  InMemoryUserDetailsManager
➡ A built-in Spring Security class that stores users in memory
   instead of using a database.
   💾 Great for testing and small demos.

───────────────────────────────────────────────────────────────
🧩 WHY WE USE IT:
───────────────────────────────────────────────────────────────
🔸 This method creates two in-memory users:
    👤 user1 → Role: USER
    👤 admin → Role: ADMIN

🔸 These users can log in and access endpoints depending on
    their assigned roles.

───────────────────────────────────────────────────────────────
🛡️ @EnableMethodSecurity EXPLAINED
───────────────────────────────────────────────────────────────
✅ 1️⃣  Purpose:
➡ @EnableMethodSecurity (or older @EnableGlobalMethodSecurity)
   enables **method-level security** in your application.

✅ 2️⃣  What It Does:
➡ Allows you to use annotations like:
   - @PreAuthorize("hasRole('ADMIN')")
   - @Secured("ROLE_USER")
   - @PostAuthorize(...)

   to control access **directly on methods** in your services or controllers.

✅ 3️⃣  Why Needed in customSecurityConfig.java:
➡ Even if you configure authentication in the security filter chain,
   Spring won’t check method-level access unless you explicitly
   enable it using this annotation.

✅ 4️⃣  Example Usage:
@PreAuthorize("hasRole('ADMIN')")
public void deleteUser() {
    // only ADMIN can call this
}

───────────────────────────────────────────────────────────────
🚀 IN SHORT:
───────────────────────────────────────────────────────────────
👨‍💻 userDetailsService() → Creates users & stores them in memory
🔐 @EnableMethodSecurity → Enables role-based access on methods
✅ Together → Provide full authentication + authorization control

═══════════════════════════════════════════════════════════════════
💡 UNDERSTANDING @Bean IN SPRING
═══════════════════════════════════════════════════════════════════

🔹 STATEMENT:
When we write @Bean above any method,
➡ whatever that method returns — Spring treats it as a **Bean**.

───────────────────────────────────────────────────────────────
🧠 WHAT DOES THAT MEAN?
───────────────────────────────────────────────────────────────
✅ 1️⃣ Spring registers the returned object in the **ApplicationContext**
   ➡ It becomes part of Spring’s **IoC (Inversion of Control) container**.

✅ 2️⃣ The object is now a **managed bean**.
   🔹 Spring controls:
       - its creation
       - its initialization
       - its lifecycle
       - its injection (when needed by other beans)

✅ 3️⃣ So when you do this:
@Bean
public UserDetailsService userDetailsService() {
    ...
    return new InMemoryUserDetailsManager(user1, admin);
}

➡ Spring internally does something like:
Register bean with name = "userDetailsService"
Type = UserDetailsService
Value = new InMemoryUserDetailsManager(...)

✅ 4️⃣ Now, anywhere in your project, you can **autowire** it:
@Autowired
private UserDetailsService userDetailsService;

➡ You’ll get the same singleton instance created by the @Bean method.

───────────────────────────────────────────────────────────────
🌱 WHY SPRING MANAGES LIFECYCLE:
───────────────────────────────────────────────────────────────
🌀 Lifecycle management means:
   - Spring decides when to **create**, **initialize**, and **destroy** beans.
   - You can even define hooks like:
     - @PostConstruct → runs after bean creation
     - @PreDestroy → runs before bean destruction

✅ This ensures **dependency injection**, **reusability**, and **consistency**
   across the whole Spring application.

───────────────────────────────────────────────────────────────
📘 QUICK SUMMARY:
───────────────────────────────────────────────────────────────
🔸 @Bean → tells Spring “Hey, manage this object for me.”
🔸 Returned object → becomes a managed bean inside the IoC container.
🔸 Spring controls its lifecycle & dependencies.
🔸 Makes your app modular, testable, and maintainable.

───────────────────────────────────────────────────────────────
🚀 EXAMPLE CONTEXT:
───────────────────────────────────────────────────────────────
@Bean
public UserDetailsService userDetailsService() {
    // 👇 This returns an object that Spring will manage
    return new InMemoryUserDetailsManager(user1, admin);
}

➡ So when Spring Security starts up, it will automatically
   use this bean to fetch user details during authentication.

═══════════════════════════════════════════════════════════════════
✨ IN SHORT:
🧱 @Bean → Registers the returned object as a managed bean
🔧 Spring → Controls its lifecycle
🔐 Benefit → Centralized, reusable, dependency-injected configuration
═══════════════════════════════════════════════════════════════════
