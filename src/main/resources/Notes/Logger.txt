═══════════════════════════════════════════════════════════════
🔍 WHAT IS A LOGGER (IN SIMPLE TERMS)
═══════════════════════════════════════════════════════════════

A **logger** is just a tool that helps you **print messages** from your code
to the console or log files — like `System.out.println()`, but smarter.

It tells you what’s happening inside your program while it’s running.

Example:
---------------------------------------------------------------
logger.info("Application started");
logger.debug("Authorization Header : {}", bearerToken);
logger.error("Something went wrong!", e);
---------------------------------------------------------------

💡 Think of it as your program’s **black box recorder** ✈️
— it keeps a record of what your code is doing.

═══════════════════════════════════════════════════════════════
🧠 WHY LOGGER USES "{}" INSTEAD OF "+"
═══════════════════════════════════════════════════════════════

When you write this:
---------------------------------------------------------------
logger.debug("Authorization Header : {}", bearerToken);
---------------------------------------------------------------
It means →
“Print this message, and only when needed, replace `{}` with the value.”

If you write this:
---------------------------------------------------------------
logger.debug("Authorization Header : " + bearerToken);
---------------------------------------------------------------
Java will **join the strings first**, even if the message
won’t actually be shown in the logs.

═══════════════════════════════════════════════════════════════
⚙️ WHAT “LOGGING TURNED OFF” MEANS
═══════════════════════════════════════════════════════════════

In Spring Boot, you can choose which types of logs to show by level.

Example (in `application.properties`):
---------------------------------------------------------------
logging.level.root=INFO
---------------------------------------------------------------

This means:
- Only **INFO**, **WARN**, and **ERROR** messages appear.
- **DEBUG** messages are hidden (turned off).

So when DEBUG is off:
- `"+"` → Still joins strings in memory (wastes a bit of time).
- `"{}"` → Does nothing — completely skips the work.

═══════════════════════════════════════════════════════════════
🧩 SIMPLE EXPLANATION
═══════════════════════════════════════════════════════════════

If DEBUG logging is off:

With `"+"`:
---------------------------------------------------------------
logger.debug("Authorization Header : " + bearerToken);
---------------------------------------------------------------
🚫 Java still builds the string: "Authorization Header : Bearer xyz..."

With `"{}"`:
---------------------------------------------------------------
logger.debug("Authorization Header : {}", bearerToken);
---------------------------------------------------------------
✅ Logger checks if DEBUG is on.
If it’s off, it skips building the message entirely.

═══════════════════════════════════════════════════════════════
🏁 IN SIMPLE WORDS
═══════════════════════════════════════════════════════════════
👉 A **logger** is just a smarter `println()` used for debugging or tracking.
👉 `"{}"` is a placeholder — it fills in values **only when needed**.
👉 `"+"` always joins strings — even if the message won’t show.

✅ Using `"{}"` = faster, cleaner, and more efficient logging.

═══════════════════════════════════════════════════════════════
